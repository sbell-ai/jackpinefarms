Add “Accounting” to the Admin dashboard (store admin) and wire it to the existing accounting/expenses work.

Requirements

1) Admin navigation

- Add a new sidebar section: “Accounting”
- Links:
    - Egg Log (Daily Log, Inventory, Adjustments) (existing routes from egg accounting UI task)
    - Expenses (new page placeholder if not built yet)
    - Reports (new page placeholder if not built yet)

2) Admin Dashboard widgets (read-only v1)

On the main Admin Dashboard page, add an “Accounting” panel with these widgets:

A) Egg Inventory Snapshot

- Data source: `GET /admin/egg-inventory/on-hand`
- Display:
    - Total eggs on hand (sum of all egg types, “each”)
    - Total dozens (floor(totalEach/12))
    - Breakdown by egg type (name + each + dozens)
- Include a “View Egg Log” button linking to `/admin/egg-log` and “View Inventory” linking to `/admin/inventory`

B) Production (last 7 days)

- Data source: `GET /admin/egg-collection?fromDate=YYYY-MM-DD&toDate=YYYY-MM-DD`
- Display:
    - Total collected last 7 days (each)
    - Simple sparkline/bar chart if easy; otherwise a compact list by day
- Link: “Add Daily Log” → `/admin/egg-log`

C) Allocation Exceptions (action needed)

- Data source: use existing Orders endpoint(s)
- Logic: list orders that contain egg products but have no egg allocations yet
- Display: up to 5 orders with status + created date + “Allocate” CTA that links to the order detail page (where the Allocate Inventory button lives)

3) Expenses + accounting (if expenses endpoints/pages are not built yet)

- Add dashboard placeholders:
    - “Expenses: coming next” tile with a link to `/admin/expenses` (page can show “Not implemented yet”)
    - Do NOT fake data; do not block the egg widgets on expenses work.

Definition of Done

- Admin sidebar shows “Accounting” section.
- Admin Dashboard shows the Accounting panel with the three widgets above.
- Widgets use generated React Query hooks (from codegen) and are protected by admin auth like other admin pages.
- No TypeScript errors; dashboard loads quickly and gracefully handles empty states (“No egg types yet”, “No collections yet”, etc.).

Implementation notes

- Follow existing patterns in `AdminLayout.tsx` and the current Dashboard page for layout, cards, loading states, and error handling.
- Use “each” for all calculations; dozens are display-only (floor(each/12)).
-