Implement Plan #6 (Egg Accounting Admin UI & E2E Test) with these clarifications:

1) Admin pages (React 18 + TS + Shadcn + RHF + Zod + TanStack Query v5) under `/admin` using generated hooks from `lib/api-client-react/src/generated/api.ts`:

- `/admin/egg-log` (Daily Egg Log): form to create collection (egg type required, date required, count required integer >= 0, notes optional). If flocks are reliably seeded and `flock_id` is supported, include an optional flock dropdown; otherwise omit flock from v1. Below: table of recent collections with columns Date, Egg Type, Each, Doz. (floor(each/12)).
- `/admin/inventory` (Inventory On Hand): table from on-hand endpoint with Egg Type, Each on hand, Doz. on hand (floor), and a status chip (“Low” if < 12 else “OK”). Keep this page inventory-only; do not add “Allocate per order” controls here (allocation happens on order detail).
- `/admin/egg-adjustments`: form (egg type required, qty_each signed integer required, reason required non-empty string). Block submit if reason missing. Below: adjustments history table.

2) Order detail page:

- Add an “Egg Inventory Allocations” panel shown only when the order contains egg line items (use a deterministic rule like `orderItem.eggTypeId != null` or `product.category === "egg"` depending on existing API response).
- Fetch `GET /admin/orders/{orderId}/egg-allocations` and list lot date, egg type, qty allocated.
- Add “Allocate Inventory” button calling `POST /admin/orders/{orderId}/allocate-eggs`; on success refetch allocations; on HTTP 400 show server message inline; if 409 already allocated show a non-error info message.

3) Admin sidebar + routing:

- In `AdminLayout.tsx`, add “Egg Log” nav group with links to `/admin/egg-log`, `/admin/inventory`, `/admin/egg-adjustments` (use lucide-react `Egg` icon).
- Register routes in `App.tsx` inside the AdminLayout wrapper (Wouter routing).

4) E2E test (API-driven fixtures):

Write and run a test that:

a) Creates an egg type via `POST /admin/egg-types`.

b) Logs a daily collection of 120 eggs via `POST /admin/egg-collection`; assert response includes created lot id; assert on-hand returns 120 each and 10 dozens.

c) Creates (or reuses) a product that maps to that egg type using the existing product creation flow/API, then creates an order with an order_item quantity 24 eggs through the existing order flow.

d) Calls `POST /admin/orders/{orderId}/allocate-eggs`; assert allocations exist; assert on-hand is now 96 each (8 dozens).

e) Creates an adjustment `qty_each = -12` with a reason; assert on-hand drops to 84 each.

f) Verifies `GET /admin/orders/{orderId}/egg-allocations` returns the correct lot date + qty.

g) Attempts to allocate 200 eggs when only 84 remain; assert HTTP 400 with a meaningful error message.

Ensure the test runs against a test database/environment (no production seeding) and passes reliably.